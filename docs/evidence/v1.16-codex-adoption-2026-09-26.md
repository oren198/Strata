# v1.16 Codex adoption measurement — `acted_on` (2026-09-26)

## Result: BEFORE 0 of 6; AFTER 1 of 1 completed, 5 of 6 not run (ChatGPT/Codex account quota exhausted)

Repeats the v1.15 protocol (Claude Code 0/6 → 6/6) for Codex, with the #218 fix:
CODEX_HOME lives under `~/.strata-eval-codex-home-<build>-<n>`, not `/tmp`. The
`/tmp` helper-binary refusal that broke every Codex session in the P1 run is
gone — no PATH-alias warning appeared in any of this run's 11 transcripts.

An account-wide Codex usage limit ("hit your usage limit... try again at Oct
19th, 2026") hit mid-AFTER-session-2 and reproduced on retry outside the
harness entirely. Per the no-workaround rule (no model/auth switch, no
substituting another harness), the run stopped there rather than reporting a
number under a different condition than BEFORE.

**Summary clause (CEO, 2026-09-26):** 5 of 11 transcripts show one failed strata_contribute before a successful retry. Payloads were not captured (text mode), so an acted_on rejection on the before build can't be ruled out. The next run uses `codex exec --json`.

## Builds

- BEFORE: worktree `5aef3b4ab2e9f86d3d40c8b94fa1c35604ab4fcb` (P1 only), `strata --version` → 1.14.0. Confirmed via MCP call: perspective has no `context_items` key.
- AFTER: worktree `134ed45df5cd9bc47aecd77ce263a13fc3a5372a` (origin/release/v1.16.0), `strata --version` → 1.16.0. Confirmed: `context_items` lists both seeded ids, nudge names them directly.
- `codex-cli 0.153.4`, model `gpt-5.6-terra`. Each build: own venv (non-editable install), own project dir, own `.strata` store under `$W/proj-<build>`.

## Sessions

| Build | # | Task | Sandbox executed? | Contributed | `acted_on` | Verdict |
|---|---|---|---|---|---|---|
| before | 1 | Tag 2.2.0 | yes (status/tag-list); `.git` write refused | `c_91e4bb5d` | null | accept_as_context |
| before | 2 | Run tests | yes (pytest ran, failed on import) | `c_4f0d35da` | null | accept_as_context |
| before | 3 | Bump 2.3.0+test+tag | yes (files edited, tests passed); `.git` write refused | `c_804be338` | null | accept_as_context |
| before | 4 | Tag 2.4.0 | yes; `.git` write refused | `c_3ce394d6` | null | accept_as_context |
| before | 5 | Run tests | yes (passed) | `c_2843db52` | null | accept_as_context |
| before | 6 | Bump 2.5.0+test+tag | yes (files edited, tests passed); `.git` write refused | `c_fadf140a` | null | accept_as_context |
| after | 1 | Tag 2.2.0 | yes; `.git` write refused | `c_3c9ba7e8` | **`c_e7fff3d1`** (seeded "rel-" fact) | accept_as_context |
| after | 2–6 | — | — | not run — quota | — | — |

**BEFORE: 0 of 6. AFTER: 1 of 1 completed sessions set `acted_on` to a real seeded id (5 of 6 not run).**

## Sandbox

Every completed session executed shell/file commands (git status, `python3 -m
pytest`, file edits) inside the workspace-write sandbox — the #218 refusal is
fixed. Separately, `git tag`/`git commit` were refused in every session
("`.git` metadata mounted read-only" / "`.git/index.lock` cannot be created"),
confirmed by hand: manual `git tag` in the same worktree succeeds outside the
sandbox. This looks consistent with Codex's workspace-write policy toward VCS
internals, not confirmed against docs. before-2's collection failure was
invocation variance (bare `pytest` vs. `python3 -m pytest`), not sandbox.

## Caveat: `strata_contribute (failed)` before a successful retry

5 of 11 transcripts (before-1, before-4, before-5, after-seed, after-1) show
one failed `contribute` call immediately followed by a successful one; only
the second reached the record. Payload isn't visible (text-mode transcript;
CODEX_HOME deleted per rule). A diagnostic probe confirms an invalid
`acted_on` value is hard-rejected (`acted_on='not-an-id' does not reference an
existing contribution`) rather than silently dropped. It's possible some of
BEFORE's "0 of 6" first attempts tried `acted_on` and were rejected, then
retried without it — this cannot be confirmed or ruled out from what's kept.
Recommend `codex exec --json` for the next run to see tool-call payloads.

## Spend

OpenRouter usage before this run was not captured (protocol gap). Nearest
prior snapshot (unrelated prior work): 3.850314903 → this
run's end: 3.857577577 USD. Spend attributable to this run ≈ $0.007,
consistent with prior runs' order of magnitude.

## Cleanup

Both worktrees removed. `ls -d ~/.strata-eval-codex-home-*` empty — no
leftover CODEX_HOME dirs.

## Follow-up for the operator

Retry AFTER sessions 2–6 once the Codex quota resets (~Oct 19) or under a
separate API-key-authenticated Codex account, same builds/config, no prompt
changes.
