# v1.15 P1b adoption measurement — `acted_on` (2026-09-25)

Run window (record timestamps): 2026-09-24 20:19–20:25 UTC. "Unprompted" =
no task prompt mentioned Strata, memory, `acted_on`, or outcomes — only the
engine's own read-time nudge could have named the mechanism.

## Result: 6 of 6 sessions set `acted_on` unprompted

Every neutral-task session (M=6, Claude Code only) contributed back to
memory, and every one of those contributions carried the structured
`acted_on` field pointing at a real context item — up from 0 of 6 in the
P1 run.

## Why: the named-ids nudge fires on the first read

`strata_read_perspective`'s self layer now returns `context_items`
(`{id, label}` per accepted-context contribution still present), and the
read-time nudge fires from the very first read (`NUDGE_MIN_READS=1`,
before any write-back) naming those ids directly. The 6 counted
transcripts are text-mode (no tool payloads), so the nudge isn't directly
visible in them; quoted instead from a post-run diagnostic read (excluded
from N/M), same mechanism:

> "...This session read c_b38bc4685274ae27 (Release tags...), c_e21dcf97101c4632 (TZ=UTC...)...; if you acted on one, pass acted_on=\<that id\> and say what happened."

Verified per session rather than assumed: each session's state file
(`SessionStateStore`) shows `reads=1` at first contribute for all 6 —
the nudge's firing precondition held every time.

## Build under test

`<workspace>/Strata-p1b-study`, branch `verify/v15-p1b` @ `1184ec6672f71ad2101e7f26e879e240f242c7af`, installed non-editably into `$W/venv` (Python 3.11.15). `strata --version` → `1.14.0` (pyproject not bumped; sha is the real identity). `claude --version` → `2.1.282`. Judge: `qwen/qwen3-235b-a22b-2507` via OpenRouter (confirmed by `strata doctor`). P1b confirmed live before measuring: a scripted `compose_perspective` call showed the self layer's `context_items` listing both seeded ids with labels.

## Seeding

Seed session (`sess_auto_69733`, excluded from N/M): A `c_b38bc4685274ae27` ("...prefix rel-...") and B `c_e21dcf97101c4632` ("...TZ=UTC...") — both `accept_as_context`.

## Sessions

| # | Task | Acted on A/B (behavior) | Contributed | `acted_on` → target | Verdict |
|---|---|---|---|---|---|
| 1 | Tag release 2.2.0 | A (tagged `rel-2.2.0`) | `c_4129f626` | → A | accept_as_context |
| 2 | Run tests | B (ran `TZ=UTC`, passed) | `c_60232dcf` | → B | accept_as_context |
| 3 | Bump 2.3.0, test, tag | A+B (`rel-2.3.0`, `TZ=UTC` passed) | `c_0d2710ee` | → A | accept_as_context |
| 4 | Tag release 2.4.0 | A referenced but **not executed** — asked for direction instead of tagging (see failures) | `c_f36bfd56` | → session 3's own contribution (`c_0d2710ee`), a live context item at S4's read time — a correction, not a direct A/B hit | accept_as_context |
| 5 | Run tests | B (ran `TZ=UTC`, passed) | `c_87bab5e2` | → B | accept_as_context |
| 6 | Bump 2.5.0, test, tag | A+B (`rel-2.5.0`, `TZ=UTC` passed) | `c_1994881a` | → A | accept_as_context |

No `acted_on` contribution was rejected or hit a validation message; all six were `accept_as_context` with no reason text specifically about the `acted_on` rule.

`strata stats writeback --include-open`: claude-code 8 sessions, 7 contributed (88%), 1 closed out (the post-run diagnostic), 100% accounted for.

## Budget / failures

OpenRouter usage `2.092660872` → `2.099428662` USD (≈$0.0068, under the $0.05 cap). Session 4 did not execute its tag (asked the user to pick among 3 options rather than acting) — a task-completion gap, not a measurement failure; it still contributed with `acted_on` set. No live store or `~/.codex` touched; all commands run from `$W/proj`.

## Caveats (read before quoting the number)

1. This measures that agents **reference** the item they acted on. It does not measure whether they report held/failed with an observable result. That is P3's closure rule and is not yet enforced.
2. Session 4 pointed `acted_on` at session 3's accepted contribution rather than at a seed. That is a valid item, so it counts. It also did not finish its task: a task gap, not a measurement one.
3. n=6, one harness (Claude Code 2.1.282), a toy repo. Paired with the P1 run (0 of 6, `v1.15-p1-adoption-2026-09-24.md`), same protocol, where no ids were shown.

Count verified by the architect from the record dump (`acted_on` non-null in 6 of 6 measured sessions), not from the runner's summary.
