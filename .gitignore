__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.venv/
venv/
.env
*.db
*.sqlite*

# Claude Code agent worktrees (created by the harness, not project state)
.claude/worktrees/

# Local Strata workspace for developing-on-Strata sessions (`strata register`
# run in this repo) — per-machine state, never committed.
.strata/
summaries/
# Cross-process scope lock files (issue #19, ADR 0012) — created next to
# whatever DB path a session resolves to, including the repo root when a
# stray test run resolves paths against the actual cwd.
.locks/
